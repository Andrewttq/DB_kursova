"""Обчислення розміру пачки за заданою швидкістю."""


class RateLimiter:
    """Перетворює швидкість (записів/с) на розмір пачки для одного такту.

    Приклад: швидкість 500 записів/с, такт триває 1 с -> пачка 500.
    При великих швидкостях (5000/с) пачку можна ділити на частини,
    щоб не публікувати все одним «вибухом» на початку секунди.
    """

    def __init__(self, tick_seconds: float = 1.0) -> None:
        self._tick_seconds = tick_seconds

    def batch_size(self, rate_per_sec: int) -> int:
        """Скільки записів прочитати за один такт при заданій швидкості."""
        raise NotImplementedError
