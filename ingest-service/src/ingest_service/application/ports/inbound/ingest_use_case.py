"""Вхідний порт — що вміє цей сервіс.

Вхідний порт описує можливості застосунку з точки зору зовнішнього світу.
Вхідний адаптер (у нас — IngestScheduler) знає лише про цей інтерфейс
і не знає, який саме клас його реалізує.
"""

from abc import ABC, abstractmethod


class IngestUseCase(ABC):
    @abstractmethod
    async def run_tick(self) -> int:
        """Виконати один «такт» роботи (раз на секунду).

        Має: дізнатися поточну швидкість, прочитати відповідну кількість рецептів,
        опублікувати їх у брокер, оновити лічильник прочитаних.
        Повертає кількість опублікованих рецептів.
        """
