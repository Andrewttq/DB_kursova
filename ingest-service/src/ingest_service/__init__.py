"""ingest-service — джерело даних системи.

Роль у системі: щосекунди читає з CSV-файлу пачку рецептів (розмір пачки залежить
від швидкості, яку користувач задає слайдером на UI) і публікує їх у чергу RabbitMQ.
Сам у бази даних нічого не записує — це робота writer-service.

Структура пакета (гексагональна архітектура):
    domain/                 — доменні класи (без залежностей від бібліотек)
    application/ports/      — інтерфейси: вхідні (що вміє сервіс) і вихідні (що йому потрібно)
    application/services/   — бізнес-логіка
    adapters/inbound/       — хто запускає логіку (планувальник)
    adapters/outbound/      — реалізації вихідних портів (CSV, RabbitMQ, Redis)
    container.py            — збирання всіх об'єктів разом (DI вручну)
    main.py                 — точка входу
"""
