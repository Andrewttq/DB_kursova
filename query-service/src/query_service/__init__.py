"""query-service — Query-сторона CQRS і веб-інтерфейс.

Роль у системі: лише ЧИТАЄ дані, нічого не записує в основні сховища
(окрім кешу, журналу пошуку та налаштування швидкості в Redis).
    Redis         — кеш результатів пошуку, статистика, журнал запитів, швидкість;
    Elasticsearch — повнотекстовий пошук;
    MongoDB       — картки рецептів і профілі шефів за id;
    Neo4j         — рекомендації та «інші рецепти шефа»;
    RabbitMQ      — лише довжина черги для статистики (через HTTP API).

Веб-інтерфейс — HTML-шаблони Jinja + HTMX: сторінка сама раз на секунду
запитує фрагмент зі статистикою і підставляє його, без написання JavaScript.
"""
